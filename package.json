{
  "name": "@vercel/bolt",
  "version": "1.0.8",
  "description": "Bolt Receiver for Vercel",
  "keywords": [
    "vercel",
    "bolt",
    "slack"
  ],
  "homepage": "https://github.com/vercel/vercel-bolt",
  "bugs": {
    "url": "https://github.com/vercel/vercel-bolt/issues"
  },
  "author": "Vercel",
  "repository": {
    "type": "git",
    "url": "https://github.com/vercel/vercel-bolt.git"
  },
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "prebuild": "npm run clean",
    "clean": "rm -rf dist",
    "ci": "npm run build && npm run test",
    "test": "vitest run",
    "dev": "vitest watch",
    "release": "changeset version && changeset publish",
    "prepublishOnly": "npm run ci"
  },
  "dependencies": {
    "@slack/logger": "^4.0.0",
    "@vercel/functions": "^2.2.4",
    "tsscmp": "^1.0.6"
  },
  "peerDependencies": {
    "@slack/bolt": "^4.4.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/node": "^22.10.5",
    "@types/tsscmp": "^1.0.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
